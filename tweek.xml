    <?xml version="1.0" encoding="utf-8"?>
    <unattend xmlns="urn:schemas-microsoft-com:unattend">

    <settings pass="oobeSystem">
        
        <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64" 
        publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">

        <!-- Skip Microsoft Account -->
        <OOBE>
            <HideEULAPage>true</HideEULAPage>
            <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
            <HideWirelessSetupInOOBE>false</HideWirelessSetupInOOBE>
            <NetworkLocation>Work</NetworkLocation>
            <ProtectYourPC>3</ProtectYourPC>
        </OOBE>

        <!-- Local Account -->
        <UserAccounts>
            <LocalAccounts>
            <LocalAccount wcm:action="add">
                <Name>Developer</Name>
                <Group>Administrators</Group>
            </LocalAccount>
            </LocalAccounts>
        </UserAccounts>

        </component>

    </settings>

    <settings pass="specialize">

        <component name="Microsoft-Windows-Deployment" processorArchitecture="amd64" 
        publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">

        <RunSynchronous>

            <!-- Disable Telemetry -->
            <RunSynchronousCommand wcm:action="add">
            <Order>1</Order>
            <Path>reg add HKLM\SOFTWARE\Policies\Microsoft\Windows\DataCollection /v AllowTelemetry /t REG_DWORD /d 0 /f</Path>
            </RunSynchronousCommand>

            <!-- Disable Ads -->
            <RunSynchronousCommand wcm:action="add">
            <Order>2</Order>
            <Path>reg add HKCU\Software\Microsoft\Windows\CurrentVersion\AdvertisingInfo /v Enabled /t REG_DWORD /d 0 /f</Path>
            </RunSynchronousCommand>

            <!-- Disable Background Apps -->
            <RunSynchronousCommand wcm:action="add">
            <Order>3</Order>
            <Path>reg add HKCU\Software\Microsoft\Windows\CurrentVersion\BackgroundAccessApplications /v GlobalUserDisabled /t REG_DWORD /d 1 /f</Path>
            </RunSynchronousCommand>

            <!-- Disable Automatic Updates -->
            <RunSynchronousCommand wcm:action="add">
            <Order>4</Order>
            <Path>reg add HKLM\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU /v NoAutoUpdate /t REG_DWORD /d 1 /f</Path>
            </RunSynchronousCommand>

            <!-- Disable Cortana -->
            <RunSynchronousCommand wcm:action="add">
            <Order>5</Order>
            <Path>reg add HKLM\SOFTWARE\Policies\Microsoft\Windows\Windows Search /v AllowCortana /t REG_DWORD /d 0 /f</Path>
            </RunSynchronousCommand>

        </RunSynchronous>

        </component>

    </settings>

    </unattend>